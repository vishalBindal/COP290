How to use makefile:
make : creates objects and executables of main shell and helper file in the folder obj and exe respectively
make clean: delete .o and executable files
make cleanAll: delete obj and exe folders
make run: Run the shell
